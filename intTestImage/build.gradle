apply plugin: 'org.akhikhl.wuff.eclipse-ide-app'

dependencies {
  compile project(':PhoneBookExample')
  compile project(':PhoneBookExample-test')
    compile "${eclipseMavenGroup}:org.eclipse.pde:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.build:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.core:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.doc.user:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.junit.runtime:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.runtime:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.ui:+"
    compile "${eclipseMavenGroup}:org.eclipse.pde.ui.templates:+"
    compile "${eclipseMavenGroup}:org.eclipse.jdt.junit4.runtime:+"
}

wuff {
  filterHtml = true
}

products {
  product platform: 'linux', arch: 'x86_32'
  product platform: 'linux', arch: 'x86_64'
  product platform: 'windows', arch: 'x86_32'
  product platform: 'windows', arch: 'x86_64'
}

